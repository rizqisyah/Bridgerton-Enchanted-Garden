/** One sliced sprite of Frame 244, placed in band-local design px. */
export type BandLayer = {
  /** Global Figma child order — bands share one stacking context, so this is comparable across them. */
  z: number
  id: string
  src: string
  x: number
  y: number
  w: number
  h: number
}
